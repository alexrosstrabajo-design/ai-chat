feat: prepare for Vercel deployment (single-function API, drop Express server)

- api/chat.js: rewrite as a Web Standard fetch handler on the Node runtime.
  Streams Groq responses back as SSE; replaces both the old api/chat.js
  (non-streaming) and the local-only server.js.
- server.js: delete. Was a local dev convenience; Vercel will route /api/chat
  to api/chat.js, no separate Node process needed.
- vite.config.js: drop the dev-only /api proxy to localhost:3001.
- package.json: remove the now-dead "server" script.
- .env.example: note that production env vars are set in the Vercel dashboard,
  not via .env.